#include <bits/stdc++.h>

using namespace std;

void test_case() {
    int n;
    cin >> n;
    cout << n - 1 << endl;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    freopen("A.in","r", stdin);

    int k;
    cin >> k;

    while (k--) {
        test_case();
    }

    return 0;
}